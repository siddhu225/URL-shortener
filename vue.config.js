module.exports = {
  devServer: {
    // host: 'localhost',
    // port: 8080,
    // headers: {
    //   'Access-Control-Allow-Origin': '*',
    // },
    // proxy: `http://localhost:3002`,
    watchOptions: {
      poll: true,
    },
  }
}